import { Component } from '@angular/core';

/** Stub para avatar ngx-ds (modal de imagem). */
@Component({
    selector: 'app-image-view-modal-stub',
    template: '',
    standalone: true,
})
export class imageViewModalComponent {}
