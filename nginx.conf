# Raiz do app no subdomínio de produção não deve exibir a landing (mesma SPA).
# Easypanel/proxy costuma repassar o host público em X-Forwarded-Host, não em $host.
map $host $host_is_app {
  default 0;
  ~*^app\.gestgo\.com\.br(:\d+)?$ 1;
}

map $http_x_forwarded_host $xff_is_app {
  default 0;
  ~*^app\.gestgo\.com\.br 1;
}

map $http_x_original_host $xorig_is_app {
  default 0;
  ~*^app\.gestgo\.com\.br 1;
}

server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location = / {
    set $app_root_redirect "${host_is_app}${xff_is_app}${xorig_is_app}";
    if ($app_root_redirect ~ "1") {
      return 302 /autenticacao;
    }
    try_files $uri /index.html;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }

  location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
    expires 30d;
    add_header Cache-Control "public, immutable";
    try_files $uri =404;
  }
}
